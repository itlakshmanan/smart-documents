server:
  port: 8081

spring:
  application:
    name: book-inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/smart_documents?useSSL=false&allowPublicKeyRetrieval=true
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect 